---
- name: "install apt packages"
  become: true
  ansible.builtin.apt:
    install_recommends: true
    name: "{{ item }}"
  loop:
    - vim
    - fish
    - zoxide
    - exa
    - stow
    - fzf
    - jq
    - fd-find
